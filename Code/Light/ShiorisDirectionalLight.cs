using UnityEngine;

namespace hUtility.Light
{
    public class ShiorisDirectionalLight : MonoBehaviour
    {
        // Use this for initialization
        private void Start()
        {
        }

        // Update is called once per frame
        private void Update()
        {
        }
    }
}